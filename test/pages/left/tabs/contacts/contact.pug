mixin contact(avatar, name, status)
    div.contact.shadow-sm.p-4.mb-3
        img.rounded-circle(src=`${avatar}` height="45" width="45")
        div.contact-body
            h6.title= name
            p.small.text-muted.text= status